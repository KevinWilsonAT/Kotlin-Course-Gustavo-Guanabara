fun main(args: Array<String>) {

    // Inputs with type validation

    /*
    *    -  To avoid input alphabetic characters in numbers spaces:
    *
    *       -   Declare a number-type variable/constant
    *       -   Insert and attribute to the variable/constant the command readline() to read the input
    *       -   Insert the null-check (!!) after the readline() command to prevent a empty input
    *       -   Insert on of the below commands after readLine() to return either a number or null:
    *           -   If the variable/constant type is Short, use toShortOrNull()
    *           -   If the variable/constant type is Byte, use toByteOrNull()
    *           -   If the variable/constant type is Int, use toIntOrNull()
    *           -   If the variable/constant type is Long, use toLongOrNull()
    *           -   If the variable/constant type is Double, use toFloatOrNull()
    *           -   If the variable/constant type is Float, use toDoubleOrNull()
    *
    *       -   Insert Elvis Operator (?:) after one of the above commands to return 0 if it returns null
    *
    *       -   This is the result of the above instructions (the example below uses Int variable/constant type):
    *           var/val <variable/constant_name>:Int = readLine()!!.toIntOrNull()?:0
    */
}