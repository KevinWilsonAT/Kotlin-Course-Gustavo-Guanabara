fun main(args: Array<String>) {

    // Using variables in Kotlin

    // var <variable_name>:<variable_type> = <variable_value>

    /*
    *   Kotlin Rules
    *
    *       - Variables names can start a character (a~z) or underline (_)
    *       - Variables names are case-sensitive ('A' is different from 'a')
    *       - Variables names only uses characters (A~Z/a~z), numbers (0~9) and underline (_)
    *       - Variables names can use accents (` ´ ~ ^ ¨ etc.)
    *       - Variables names can't use spaces ( )
    *       - Variables names can't use symbols, except underline (_)
    *       - Variables names can't use a reserved word
    */
}