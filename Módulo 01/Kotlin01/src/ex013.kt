fun main(args: Array<String>) {

    // Kotlin Arithmetic Operators

    /*
    *       Symbol      Kotlin Command      Function
    *       ###############################################
    *       +           .plus()             Addition
    *       -           .minus()            Subtraction
    *       *           .times()            Multiplication
    *       /           .div()              Division
    *       %           .mod()              Module (for decimal/real numbers)
    *       %           .rem()              Module (for integer numbers)
    *
    *       Use the commands toFloat() or toDouble(), to enable a decimal division between two integer numbers,
    *
    *       Int / Int = Int (ex.: 5 / 2 = 2)
    *       Int (using toFloat() or toDouble()) / Int = Float or Double (ex.: 7 (using toFloat()) / 2 = 3.5 (float))
    *       Int / Int (using toFloat() or toDouble()) = Float or Double (ex.: 7 / 2 (using toFloat()) = 3.5 (float))
    *
    *       Float / Float = Float (ex.: 4.5 / 1.5 = 3.0)
    *       Float (using toDouble()) / Double = Double (ex.: 4.5 / 1.5 = 3.0)
    *       Double / Float (using toDouble()) = Double (ex.: 4.5 / 1.5 = 3.0)
    *
    *       Double / Double = Double (ex.: 4.5 / 1.5 = 3.0)
    *       Double (using toFloat()) / Float = Float (ex.: 4.5 / 1.5 = 3.0)
    *       Float / Double (using toFloat()) = Float (ex.: 4.5 / 1.5 = 3.0)
    */
}